export enum USER_ROLES {
  ADMIN = "admin",
  SUPER_ADMIN = "super_admin",
  USER = "user",
  SELLER = "seller",
}

export enum GENDER {
  MALE = "male",
  FEMALE = "female",
  OTHER = "other",
}

export enum STATUS {
  ACTIVE = "active",
  INACTIVE = "inactive",
}

export enum DOCUMENT_TYPE {
  NID = "nid",
  PASSPORT = "passport",
}
