export enum CHAT_STATUS {
  ACTIVE = "active",
  DELETED = "deleted",
}

export enum CHAT_COMMUNICATION_TYPE {
  PRODUCT = "product",
  SERVICE = "service",
  STORE = "store",
  SUPPORT = "support",
  OTHER = "other",
}
